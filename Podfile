# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!
target 'FirebaseUIDemo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for FirebaseUIDemo
  pod 'FirebaseUI', '~> 5.0'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'TwitterKit'
  
  target 'FirebaseUIDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FirebaseUIDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
